export enum RouteType {
  Home = '/',
  Login = '/login',
  Logout = '/logout',
}

export enum LocalStorage {
  Token = 'ac',
}

export enum RequestPaths {
  Tokens = '/v1/tokens',
  Servers = '/v1/servers',
}
